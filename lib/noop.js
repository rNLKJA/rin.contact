// Intentionally empty — replaces Next.js's built-in polyfill-module at build time.
// Safe because our browserslist targets (Chrome ≥93, Firefox ≥92, Safari ≥15.4,
// Edge ≥93) natively support every feature the polyfill-module patches:
// String.trimStart/trimEnd, Array.flat/flatMap/at, Object.fromEntries/hasOwn,
// Promise.finally, URL.canParse.
