{
  "scripts": {
    "setup": "npm install",
    "run": "npx next dev --webpack -p ${CONDUCTOR_PORT:-3000}",
    "build": "next build --webpack",
    "archive": "rm -rf .next node_modules"
  },
  "runScriptMode": "concurrent"
}
